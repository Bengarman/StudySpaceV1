project 'Study Space.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Study Space' do

  pod 'Google-Mobile-Ads-SDK'
  pod 'Firebase/AdMob'
  pod 'ColorSlider', '~> 4.1'
  pod 'Inapptics'
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Study Space

  target 'Study SpaceTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'TimetableExt' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TimetableExt

end
